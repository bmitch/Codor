<?php

class FooBar
{
    function alpha($one)
    {
        foreach ($variable as $key => $value) {
            # code...
        }
    }
}

function beta($one)
{
    foreach ($variable as $key => $value) {
        # code...
    }
}